// A simple service to play sound effects for user actions.

// Sounds have been disabled due to user feedback and to prevent errors.

export const playCorrectSound = () => {
  // Sound disabled
};

export const playIncorrectSound = () => {
  // Sound disabled
};

export const playHeartLostSound = () => {
  // Sound disabled
};
